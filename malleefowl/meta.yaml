package:
  name: malleefowl
  version: !!str 0.1.6

source:
  fn: malleefowl-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/m/malleefowl/malleefowl-0.1.6.tar.gz
  md5: 7ef3ce3d084fcfa99aaa42b1bac40eae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - malleefowl = malleefowl:main
    #
    # Would create an entry point called malleefowl that calls malleefowl.main()

    - wpsclient=malleefowl.wpsclient:main
    - wpsfetch=malleefowl.fetch:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 2

requirements:
  build:
    - python
    - setuptools
    - pywps
    - htmltmpl
    - python-magic
    - ordereddict
    - lxml
    - owslib
    - pywget
    - wget
    - concurrentloghandler
    - nose

  run:
    - python
    - pywps
    - htmltmpl
    - python-magic
    - ordereddict
    - lxml
    - owslib
    - wget
    - concurrentloghandler
    - nose

test:
  # Python imports
  imports:
    - malleefowl

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - wpsclient --help
    - wpsfetch --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bird-house/malleefowl
  license: Apache License v2.0
  summary: 'Malleefowl simplifies the usage of WPS and has some supporting WPS processes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
